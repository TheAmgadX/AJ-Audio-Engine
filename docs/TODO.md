# TODO:

- integrate memory manager with the engine.
- review the current usage of vectors, make sure there is 0 moves and 0 copies.
- build the playing feature and the Audio IO manager.
- build the session manager and mixer.
- build the read function in file_streamer.
- build the undo system.
- finish the AJ_Engine APIs with all TODOs listed on it.
- add heavy testing.
- add resampling feature using Ffmpeg.
- trace any memory leaks, issues or fails using the aid of memory manager.
- document the whole engine with proper UMLs.
- add memory alignment and memory optimizations.